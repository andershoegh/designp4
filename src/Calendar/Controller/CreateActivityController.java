/*
package Calendar.Controller;

import javafx.event.ActionEvent;
import javafx.fxml.FXMLLoader;
import javafx.scene.Node;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.DatePicker;
import javafx.stage.Stage;
import SQL.SqlConnection;
import javafx.fxml.FXML;
import javafx.scene.control.TextField;
import java.io.IOException;
import java.sql.Connection;

import java.sql.PreparedStatement;
import java.sql.SQLException;

public class CreateActivityController {

    @FXML private DropDownButton typeInput;
    @FXML private DatePicker dateInput;
    @FXML private TextField timeInput;
    @FXML private TextField locationInput;
    @FXML private TextField notesInput;

    public void createButton(ActionEvent event){
        Connection conn = SqlConnection.connectToDB();
        if(Dropdownbutton)
    }
}

// I tvivl om hvordan vi styrer hvorhen i DB det kommer alt efter valg i dropdown menuen om hvilken
// aktivitet der er valgt.
*/
